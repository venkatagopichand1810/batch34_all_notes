// extracts the portion of an array
// it will not modify the original array

// Array.slice(starindex, endindex)..endindex will excluded

const numbers = [10, 20, 30, 40, 50];

const result = numbers.slice(1, 4);

console.log(result)
