// add, remove or replace the elements
// will modify the orignal array

//array.splice(starindex, deletcount, items.......)


const numbers = [10, 20, 30, 40, 50];

const result = numbers.splice(1, 4);

console.log(numbers)

const mobiles = ["LG", "Oppo"];
const index = mobiles.indexOf("Oppo");
console.log(index)
